{
  "name": "nodl-miniapp",
  "version": "1.0.0",
  "private": true,
  "description": "Telegram MiniApp entrypoint powered by apps/web",
  "scripts": {
    "dev": "npm --prefix ../web run dev",
    "build": "npm --prefix ../web run build",
    "start": "npm --prefix ../web run preview"
  }
}
